import { DataGrid } from '@mui/x-data-grid';

const Grid_table = ({}) => {

    return (
      <div>
          <DataGrid
          
          />
      </div>
    );
  };
  
  export default Grid_table;